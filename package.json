{
  "name": "petitoizo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Où aller voler en parapente en sécurité, selon les conditions météo",
  "scripts": {
    "build:spots": "node scripts/build-spots.js",
    "fetch:forecast": "node scripts/fetch-forecast.js",
    "build": "npm run build:spots && npm run fetch:forecast",
    "serve": "npx serve public"
  }
}
